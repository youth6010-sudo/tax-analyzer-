# 미수관리 — 고정 vs 업로드 (전체 구조)

기준일: **2026.08.31 공문 확정본** 위에 **9월~만** 얹음.  
확정본 = **2026-09-21** LIVE 공문 상태(오늘 작업분 스냅샷).

---

## 한 줄 요약

| | 무엇이 | 누가 바꾸나 |
|--|--------|------------|
| **고정** | 8월 31일까지 공문 줄(기장·조정료·입금 내역) | 업로드로 **안 바뀜**. 공문 수정모드·관리자 스크립트만 |
| **예외: 기장료만** | 미납 기장이 **1개월 이하**이고 조정료 등 없음 | 업로드할 때마다 **내역 월이 롤링**될 수 있음 |
| **업로드로 변경** | 목록 **잔액** + **9월 이후** 공문 줄 | 현황표 + 거래처별 두 파일 함께 반영 |

---

## 1. 고정되는 것 (올리지 않아도 / 올려도 유지)

### ① 공문 cutoff — `2026.08.31`
- 이 날 **포함·이전** 공문 상세는 동결(복잡미수).
- 거래처별 엑셀을 올려도 **9월~ 거래만** 공문에 추가.
- 7·8월 기장·입금을 업로드가 지우거나 다시 조립하지 **않음**(기장료만 제외).

### ② 기장료만 vs 복잡미수
| 구분 | 조건 | 업로드 시 |
|------|------|-----------|
| **기장료만** (`simple_bk`) | 미수가 기장·기타수수료만, **미납 월 ≤ 1** | 9월~ 반영 시 기존 월 줄을 버리고 **롤링 재구성** 가능 |
| **복잡미수** (`complex`) | **2개월↑** 기장 밀림, 또는 **조정료·성실·부가세** 등 포함 | **지금 고정값 유지** + 9월~만 **추가** |
| **보호** | 오프라인·하나비·에스와이 등 | 거래처별 자동 반영 **제외** |

분류·스냅샷: `lib/arrearsLetterComplexity.ts` · `data/arrears-content-frozen-codes.json` · `data/arrears-frozen-letter-lines.json`  
재스냅샷: `node --import tsx scripts/freeze-today-arrears-letters.mjs --apply`

### ③ 저번주~오늘 확정한 표시 규칙 (코드)
- **법인조정료 등**: 공문 원문 유지. asOf 연도 임의 부착 금지.
- **기장 사이에 낀 조정·성실**: 화면에서 연도 숨김 가능.
- Neon(`PREV`)으로 공문 **통째 덮어쓰기 금지**.

### ④ 예외 업체 (코드에 박힘)
| 코드 | 상호 | 동작 |
|------|------|------|
| 00130·00131·00229 | 티밸류홀딩스·매거진·예윤 | 잔액=공문합이어도 **불일치(원장)** 강제 |
| **00183**·00199 | **오프라인**·하나비 | 거래처별 자동 반영 **제외**, 공문 수동·고정 |
| **00176** | **에스와이메탈** | 거래처별 **스킵**. 선수금 대체(22만) 맨 앞 고정 |
| **00191** | **한빛이텍** | 빈 지급 100만 + 7·8월 기장 **내용 동결** |
| **00152** | **로터스인베스트먼트** | 07.27 공문 전체 + 7·8월 **내용 동결** |
| **01206** | **올바릇** | 7/31 300만·**9/15** 24.2만 입금 **내용 동결** |
| 담당=인디 | — | 거래처별 파일 **스킵**. 잔액만 현황표, 상세는 기존 공문 |

수동 확정분 복구: `node --import tsx scripts/ensure-frozen-letter-patches.mjs --apply`  
기준 파일: `data/arrears-frozen-letter-patches.json` · 스냅샷 `data/arrears-frozen-letter-lines.json`

### ⑤ 불일치 판정 공식 (고정)
```
불일치 = 현황표 잔액 ≠ 공문 Σ(금액 − 지급)
```
잔액 **음수**이고 상세합 0이면 「원장만」(세부 없음).  
가짜 줄(`미수반영(현황맞춤)`)로 맞추지 않음.

---

## 2. 업로드하면 바뀌는 것

화면: **미수관리 → 엑셀 업로드 → 두 파일 함께 반영**  
파일명: `미수수수료 거래처(잔액)현황_날짜` + `거래처별 현황_날짜`

### ① 현황표 (잔액 파일)
| 바뀜 | 안 바뀜 |
|------|---------|
| DB **잔액**·이월·차대 | 공문 줄 |
| 기준일(`statusAsOfDate`) | cutoff(항상 8/31) |
| 담당(수임처 미연결 시만) | 수임처 연결된 담당 |

### ② 거래처별 현황
| 바뀜 | 안 바뀜 |
|------|---------|
| **9월~** 기장·입금 줄 추가/교체 | **8월까지** 공문 전체(복잡미수) |
| 기장료만: 월 롤링 재구성 | 수정모드 **manual** 줄 |
| | 인디·오프라인·하나비 |

추가 규칙:
- 공문에 **기장 미납이 한 달이라도 있으면** → 매출·입금 쌍을 생략하지 않음.
- 기장 미납이 **없으면** → 같은 달 매출+입금 즉시회수 세트는 공문에 안 넣음.

---

## 3. 흐름도

```
        ┌─────────────────────────────────────┐
        │  확정본 (8/31까지 공문)  ← 고정      │
        │  (기장료만은 롤링 예외)               │
        └─────────────────────────────────────┘
                          │
     업로드 시            ▼
┌──────────────┐    ┌──────────────────────────┐
│ 현황표 엑셀  │───►│ 잔액·기준일만 갱신         │
└──────────────┘    └──────────────────────────┘
┌──────────────┐    ┌──────────────────────────┐
│ 거래처별 엑셀│───►│ 9월~ 줄만 공문에 추가     │
└──────────────┘    └──────────────────────────┘
```

---

*소스: `lib/arrearsImportConfig.ts`, `lib/arrearsImportApply.ts`, `lib/arrearsBalanceLock.ts`, `lib/arrearsLetterComplexity.ts`*
